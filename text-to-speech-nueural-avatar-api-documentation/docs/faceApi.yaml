paths:
   /face_api:
     post:
       summary: Uploads a file.
       consumes:
         - multipart/form-data
       parameters:
         - in: formData
           name: audio
           type: file
           description: upload .wav file
         - in: formData
           name: vidoe
           type: file
           description: upload .mp4 file

