paths:
   /text_to_speech:
     post:
       summary: Uploads a file.
       consumes:
         - multipart/form-data
       parameters:
         - in: formData
           name: text
           type: file
           description: upload text file

