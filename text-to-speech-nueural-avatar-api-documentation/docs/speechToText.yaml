paths:
   /speech_to_text:
     post:
       summary: Uploads a file.
       consumes:
         - multipart/form-data
       parameters:
         - in: formData
           name: audio
           type: file
           description: Upload .webm file
